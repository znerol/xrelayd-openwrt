config BR2_PACKAGE_HOST
	prompt "host.............................. used to query DNS servers for domain names and zones"
	tristate
	default m if CONFIG_DEVEL
	help
		http://packages.debian.org/stable/host
